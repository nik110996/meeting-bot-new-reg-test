package ru.meetingbot.deamon.algorithm;

/**
 * Основной метод isIt(). Должен вернуть true, если партнёры подходят для встречи, иначе false
 */
public class Algorithm {

    public boolean isIt(long userId, long partnerId) {
        return true;
    }
}
